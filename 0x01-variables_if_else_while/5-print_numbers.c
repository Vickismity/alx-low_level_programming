#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints digits of number in base 10
 *
 * Return: Always 0
 */
int main(void)
{
	int n;

	for (n = 0 ; n < 10 ; n++)
	{
		putchar(n);
	}
		putchar('\n');

		return (0);
}
