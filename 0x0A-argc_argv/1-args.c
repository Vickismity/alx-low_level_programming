#include <stdio.h>

/**
 * main - program to prints the number of arguments to it
 * @argc: argument count
 * @argv: argument vector
 * Return: 0
 */
int main(int argc, char __attribute__((unused))*argv[])
{
	printf("argc = %d\n", argc);
	return (0);
}
